'use client'

import styled from 'styled-components'

export const ProjectContainer = styled.div`
  width: 100%;
  text-align: left;
  min-height: 100vh;
  align-content: center;
  justify-items: center;

  ul {
    display: flex;
    flex-wrap: nowrap;
    gap: 10px;
    padding: 0;

  }

  .cardWidth {
    min-width: 380px; /* Define largura de cada card */
  }
`

